# CCSwitchMulti v3.19.2-5

## Codex Qwen/vLLM 推理语义

- 修复 Qwen/vLLM 的 `reasoning.effort=none` 被错误翻译为启用 thinking；Qwen 的 thinking 参数现在只在上游契约明确时覆盖。
- 增加 Qwen 推理默认值的兼容回归，覆盖显式关闭 thinking、旧配置迁移以及 vLLM chat 模板推断。

## Prompt cache 路由

- 更新多语言说明，明确稳定会话标识、已验证兼容的上游、未知字段 400 风险及 `cached_tokens` 验证方式。

完整测试验证：TypeScript、Rust `cargo check`、Rust library（3107 passed，0 failed，5 ignored）和 Vitest（137 files，1116 tests）均通过。
