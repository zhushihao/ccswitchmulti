# CCSwitchMulti v3.19.1-27

v3.19.1-27 在完整继承 v26 最终发布线的基础上，集中完成 Codex reasoning 能力统一、Sub-Agent V2 推理策略协调，以及长时间 ImageGen 请求的防重放保护。它不是对 v26 功能的重复打包：现有 v27 的 32 个独立提交已通过合流提交 `6fe99e9d` 与 v26 最终线汇合。

## 主要更新

### 1. 统一 Codex reasoning 能力来源

- Provider 原生支持档位、Codex 可选择档位、Provider 默认档位、关闭能力、来源置信度和 effort 映射现在由同一个 resolver 输出，模型目录、Sub-Agent role、预览/状态和请求转换不再分别维护推理档位。
- 未声明能力的第三方模型保持 unknown，不会继承通用 GPT 推理档位。
- DeepSeek V4 Flash/Pro 的维护能力为 Provider 原生 `low/high/max`、默认 `high`，并确认 `medium/xhigh -> high` 映射；不会凭空声明 `ultra`。
- 旧模型目录缺少 reasoning metadata 时仍可通过精确模型 ID 恢复维护能力，但显式目录能力始终优先。

### 2. Sub-Agent V2 推理策略与 UI 协调

- profile schema v2 将推理配置明确分为 `delegated`、`model_default`、`fixed` 和 `disabled`，不再把任务问卷隐式转换为固定 reasoning effort。
- legacy `auto` 平滑迁移为 delegated；legacy 显式 effort 与旧 override 迁移为 fixed，并保留字段优先级。
- role TOML、预览和状态页支持 `max`；具体可选值仍受目标模型的 capability 限制，unsupported effort 会 fail closed。
- UI 展示能力来源、Provider 接受值、Codex 可选值、默认值、映射和关闭能力；普通用户通过结构化选项配置，专家 JSON 在修改草稿前完成校验。
- v26 的父级 selection policy、preferred custom role 优先级、Codex 0.147 fork 兼容约束和 V1/V2 可逆切换全部保留。

### 3. 长时间 ImageGen 请求不再被错误重放

- Codex 图片生成/编辑是可能持续数分钟的非幂等 POST。过去 `connection_closed_before_message_completed` 被误归类为发送前失败并返回可重试 502，可能导致同一昂贵请求被再次提交。
- 现在只有 request builder 错误和 connect 阶段错误保留安全重试；请求已发送但结果未知时归类为 `ResponsePending`。
- ImageGen 的结果未知响应改为 HTTP 424、错误码 `cc_switch_image_result_unknown`、`retryable=false`，且不返回 `Retry-After`，防止 CCSM 和 Codex transport 重放。
- 共享 HTTP client 增加 HTTP/2 keepalive ping、ack timeout、idle keepalive 和自适应流控，降低长任务连接被中途回收的概率。

## v26 合流边界

- v27 原分支从 v26 的早期候选分叉。发布前已整体合入 v26 最终 30 个独立提交，保留 PowerShell 污染宿主修复、Tauri JS/Rust 配对、NSIS 安装态哈希推导、事务安装保护、图像能力持久化和 Sub-Agent 父级自动选型根修。
- 合流没有重做 v26 的 173-ref/198-patch 历史审计；该审计结论仍为 `actionable-missing=0`。本次只验证 v27 新增提交和 v26 最终线在同一树上的行为。

## 验证

- `codex_subagent_profiles`：84/84。
- `codex_config`：167/167。
- ImageGen：13/13；Codex proxy：10/10；reqwest 错误分类：3/3。
- Rust library：2991 passed / 0 failed / 2 ignored。
- Vitest：123 files / 996 tests passed。
- TypeScript typecheck、Prettier、`cargo check --lib`、rustfmt、`git diff --check` 全部通过。
- 原生 Windows PowerShell 5.1 Pester：53/53。

## 升级说明

1. 从本 Release 下载对应平台和架构的安装包，并核对 GitHub 服务端 digest 与 updater 签名。
2. Windows 覆盖安装必须由完整事务流程完成停止、卸载、安装、拉起、健康检查和失败回滚；不要在普通交互 shell 中单独结束 CCSwitchMulti。
3. 涉及 Codex Sub-Agent role 或模型目录变化时，保存当前 Provider/MultiRouter 后完整重启 Codex app-server，并新建会话读取新的工具与 role snapshot。
4. 不需要删除 CCSwitchMulti 数据库、`~/.codex` 或用户自定义 role TOML。

## 发布证明

- 正式 tag `v3.19.1-27` 的 annotated tag object 为 `afe53ad14fa3f5e84e7c697c31c5002d18ac6944`，peeled commit 为 `50c32b3731c91e50c1249821aebc9e5da4110823`，与本地 `release:local` 的 metadata commit 精确一致。
- 本地流水线在该固定提交上 exit 0，15/15 checksum 回读一致。Windows x64 raw EXE SHA-256 为 `CF31C903F52027F6B7D6E608AF6AA43FF2C1C609CBE2224D8C66F2304B008107`，NSIS installer 为 `D318CCDC6BF8CB7F7424E38FF9B359D1E4BEE6620EF9307CB3FDF74C59CDF522`，严格推导的 NSIS 安装态 EXE 为 `FA9726A634969DF11AD068E034063DBF3658694EC06078FF1F901DBB3D2B7D7D`。
- 独立隐藏事务 `ccsm-20260815-074918-c5bf859249d348ec98840a8717578637` 完成 preflight、备份、kill、卸载、安装、隐藏拉起与健康检查；结果为 `Success`，`Error=null`、`RollbackError=null`。安装后 FileVersion/ProductVersion 均为 `3.19.1-27`，运行中 EXE 与预期安装态哈希一致，`/health` 返回 HTTP 200。
- 安装版 UI 已实际进入 MultiRouter 的“子 Agent”页：顶部存在“总览 / 模型源 / 路由规则 / 子 Agent / 状态 / 测试发布”导航；V2 显示“当前使用 V2”和禁用态“已启用 V2”，V1 显示可切换“启用 V1”。Flash 能力编辑器显示 Provider 原生 `low/high/max`、Codex 可选 `none/low/medium/high/xhigh/max`、默认 `high`、允许关闭及 `medium/xhigh -> high` 映射。
- GitHub Actions run `31851926650` 全部成功：Windows x64/ARM64、Linux x64/ARM64、macOS、Publish GitHub Release 和 Assemble `latest.json` 七个 jobs 均为 `success`。
- Release 非 draft、非 prerelease，并成为 GitHub Latest。19 个实际资产全部重新下载，逐项 SHA-256 与 GitHub 服务端 digest 一致，`MismatchCount=0`。
- `latest.json` 版本为 `3.19.1-27`，包含 `darwin-aarch64`、`darwin-x86_64`、`windows-x86_64`、`windows-aarch64`、`linux-x86_64`、`linux-aarch64` 六个 updater entry；所有 URL 均指向 v27，所有签名均非空。
- 本次授权范围是 GitHub Release。仓库的 R2 `release.released` workflow 没有被 Actions 内使用 `GITHUB_TOKEN` 创建的 Release 二次触发，因此没有手动 dispatch R2 部署，也不宣称 R2 镜像已同步。
