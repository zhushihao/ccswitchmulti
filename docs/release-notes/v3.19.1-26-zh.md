# CCSwitchMulti v3.19.1-26

这是 v3.19.1-26 的正式发布说明。它继承 v3.19.1-25 的 Windows Setup hotfix，并通过 56 个可追溯提交，将并行分支中的 Provider 就绪、全局设置、MultiRouter、Sub-Agent V2、图像能力持久化和 Windows 事务发布修复收敛到同一条可验证的配置链路。

## 主要更新

### 1. Windows Setup hotfix 继续有效

- 完整继承 v25：未知第三方模型会写出必填的空 `supported_reasoning_levels`，不再因目录 JSON 缺字段使 Codex `config/read` 失败，从而阻断 Windows setup 重试前的正常启动链路。
- 此修复不会把 GPT 的通用推理档位重新套给未知模型；未声明能力仍保持保守、不注入 effort。

### 2. Provider 就绪与全局设置职责明确

- Codex Provider 表单始终呈现模型同步、默认模型、上游协议、连接验证和 MultiRouter 就绪结论；维护预设继续由 CCSwitchMulti 管理 `/model` 目录，但“由 CCSwitchMulti 维护”只表示元数据归属，不再把未经验证的当前凭据和端点标成就绪。
- 连接验证结果绑定到当前 Provider 的端点、凭据/账号、请求覆盖、协议、默认模型和模型目录能力；任一身份输入变化都会立即取消旧就绪，较早发出的探测即使更晚返回，也不能覆盖新身份的结果。
- 共享 Codex TOML 读取失败时改为 fail closed：显示真实错误和 Retry；成功读取前不挂载可编辑 TOML、Goal mode 或保存入口，避免占位内容覆盖真实配置。
- 全局设置只管理通用偏好和共享配置；Provider takeover、路由 TOML、模型目录与 `127.0.0.1` 本地入口仍由 CCSwitchMulti 的当前 Provider/MultiRouter 私有投影管理，不能写入共享 common config。

### 3. MultiRouter 四步向导与原地完成

1. 选择或新建目标 Provider，并初始化可路由模型与 V2 配置。
2. 同步模型、验证协议与连接，确认 Provider readiness。
3. 配置路由规则并启用本地接管。
4. 在状态工作台完成当前 Provider、监听端口、Codex 接管、路由入口和最近匹配转发的真实请求验证。

- 验证成功后保留在状态工作台显示结果，不会自动跳转 Sessions 或自动打开历史修复；历史修复仍是用户按需进入的独立工具。

### 4. Sub-Agent V1/V2 均保留

- V1 继续保留前五个 direct override 候选模型窗口。
- V2 继续根据完整可路由 profile/catalog 生成 managed roles，不受 V1 前五项限制；运行中的 Codex/app-server 仍需要重启并新建会话以获取新的 role snapshot。
- V2 现在会把全局 selection policy 与实际生成的 custom roles 编译为顶层托管 `developer_instructions`；匹配任务中的 `preferred` role 明确优先于通用 `default`、`worker`、`explorer`，但最终委派动作仍由 Codex 官方机制执行，CCSwitchMulti 不改写 `spawn_agent` 请求。
- 对 Codex `0.147` 的 custom-role/full-history 兼容边界，托管说明要求使用 `fork_turns=none` 或正整数；兼容失败时保持相同 `agent_type` 重试，不允许静默删除角色后退回内置 Agent。新版 Codex 仍按其自身工具描述决定 fork 行为。
- V2 profile compiler 现在读取模型 catalog 的 `supported_reasoning_levels` 与 `default_reasoning_level`：只注入目标模型支持的 effort，不支持时回退 catalog 默认并在预览/状态中给出 warning，能力未知时省略固定 effort。DeepSeek V4 Flash/Pro 的精确旧 ID 兼容项提供 `low/high/max`、默认 `high`，避免旧目录继续生成空推理能力。
- `inputModalities` 只持久化 `text` 或 `text + image`；已知模型从 catalog hydrate，未知模型保守不声明图像能力，并在手工覆盖路径提示图像任务的安全边界。
- 普通 Provider 的 `/models` 同步现在会端到端保留后端明确返回的 `inputModalities` 与 `supportsImage`，覆盖新增模型、已有模型、手动选择、Provider 保存以及 DB/live reload。即使没有 `inputModalities`，显式 `supportsImage: true` 与 `false` 也会独立保存；camelCase、snake_case 和 legacy `vision` 读入后统一为 camelCase，不会按模型名称或 modalities 猜测图像能力。

### 5. 配置写入后的 TOML 验证

- V2 initialize、reconcile 和普通更新统一确认数据库持久化、投影状态，以及每个 managed-role TOML 的绝对路径、存在性和精确内容回读。
- 当前 Provider 仅在 expected/actual TOML 一致时报告已应用；非当前 Provider 明确为 `not_required`，可重试状态保持 `pending_retry`。

## 分叉审计

- 对本地 `refs/heads/*` 与 `refs/remotes/fork/*` 共 173 个 refs 完成 exact tip、merge-base、双向 ancestry、ahead/behind 与 `git cherry` 审计。
- 35 个 refs 的 198 个去重 `+` patches 中，66 个 `fix/feat` 均已完成当前树语义审阅；最终 `actionable-missing=0`。
- 2026-08-15 通过 live `git ls-remote --heads fork` 复核远端新鲜度：远端 138 个 heads 与本地排除 symbolic HEAD 后的 138 个 tracking tips SHA 全部一致，`NEW/MOVED/DELETED=0`。本轮未 fetch，但 tips 没有漂移，因此无需更新 objects，既有 173-ref/198-patch 审计结论不变。
- 发布分支初次推送后再次复核 139 个远端 heads 与 139 个本地 tracking tips，仍为 `NEW/MOVED/DELETED=0`；新增的唯一 tip 是本发布分支自身。正式 tag 固定在 `7e6665fcfd297f3f8954850d59f9786831b1e3d2`；tag 之后的 post-release 提交（首个为 `07f1da657bf9c926eb7bac191faffb006e431b1c`）只追加本说明、审计和 memory，不构成新的历史功能分叉。

## 验证证据

- Rust `codex_subagent_profiles`：77 passed；`codex_config`：166 passed；reasoning resolver：3 passed。
- Rust library：2978 passed / 0 failed / 2 ignored（`CARGO_BUILD_JOBS=2`，`--test-threads=1`）。
- `ae764934` 的历史中间全量 Vitest 为 123 files / 990 tests passed（排除 `.worktrees`，单文件串行）；它发生在三项 Task 11A 回归加入之前。最终修复的统一 RED 为 79 项中 74 passed / 5 expected failed；GREEN 聚焦为 76/76，共享设置单独为 3/3。
- Task 11A 持久化回归 RED 为 2 files / 11 tests 中 8 passed / 3 expected failed，三项都因缺失 `supportsImage` 失败；GREEN 为 11/11，扩大 CodexFormFields、ProviderForm 与 config-state 聚焦为 6 files / 48 tests passed。
- 当前实现的完整 Vitest 为 123 files / 993 tests passed、exit 0、耗时 234.01 秒。Task 11A 独立复审在其实现 HEAD 返回 PASS；历史 `final-rereview-report.md` 的 FAIL 仅对应 `ae764934`。
- TypeScript typecheck、`cargo check --lib`、rustfmt、Prettier、Pester transaction/release tests（53/53）和 staged/unstaged diff check 均通过。
- 安装态 `71f35116` 曾完成构建与事务安装，但真实无模型名 canary 发现父 Agent 回退 `explorer` 以及 DeepSeek catalog 推理档位为空，因此该中间候选已作废。RED `35fa3b87` 与 GREEN `80e7b04a` 完成根修并通过上述 Rust/前端门禁；正式本地资产已从包含根修的 clean `7e6665fc` 重新构建、重算哈希并重新事务安装。
- 本地发布流水线第一次启动在真正编译前失败：Codex primary-runtime 继承的 `PSModulePath` 让 Windows PowerShell 5.1 优先加载 PowerShell 7 的 `Microsoft.PowerShell.Utility`，因此脚本找不到本应存在的 `New-TemporaryFile`。RED `cdeb6969` 锁定污染宿主，GREEN `39e41d70` 改用 .NET 临时文件 API 和 UTF-8 无 BOM 写入，不修改用户或系统环境。
- 第二次调用成功完成 release 编译、Vite production build、最终链接和 NSIS，但在导出 checksum 时证明同一污染模块也缺少 `Get-FileHash`；因此仍不是完整 release。RED `44705602` 增加固定 SHA-256 fixture，GREEN `9808b11e` 以流式 .NET SHA-256 同时替换 exporter 与 pipeline 的两个 hash 调用。污染宿主和原生 WinPS 均为 2/2，合并事务套件为 48/48。
- 随后的完整构建暴露 Tauri updater bundle type 警告。根因不是 NSIS 或签名失败，而是 npm `@tauri-apps/cli 2.8.1` 的 bundler 仍查找 `.taubndl` PE section，而 Cargo 图中的 `tauri-utils 2.8.3` 已按官方 `#14521` 改用 `__TAURI_BUNDLE_TYPE_VAR_UNK` 字符串标记。RED `6a513c08` 锁定协议边界，GREEN `82a4e7c1` 将 CLI 精确升级到包含字符串替换实现的 `2.10.1`；不能只因 EXE 中同时出现 `NSS`/`UNK` 文本就把旧 CLI warning 判为假阳性。新版 preflight 随即证明 core API、dialog、updater 的 JS/Rust minor 也有三组历史错位；RED `4b181977`、GREEN `65ac17c8` 将三组 JS bindings 精确对齐，实际 Tauri build 已输出 marker-based NSIS patch 日志并无旧 warning。
- 首次事务安装按设计因安装态哈希与 raw EXE 哈希不同而安全回滚。诊断证明新版 bundler 在 NSIS 内嵌 EXE 中把唯一 `...VAR_UNK` 改成 `...VAR_NSS`，打包后则恢复 raw/portable EXE；两者都是预期产物但 SHA-256 不同。RED/GREEN `e23642a3` / `591c2090` 让失败日志保留 actual/expected，`667b7572` / `d6b0d13a` 新增严格的 installed-hash 推导 helper：必须恰好一个 restored marker，否则 fail closed，不能再拿 portable/raw 哈希冒充安装态哈希。
- 前两次分别是 pre-build failure 和 post-build export failure，都不计作完整本地 release；`71f35116` 的后续构建虽然完成，但被真实 Sub-Agent canary 阻断。最终 `pnpm release:local` 已在 clean `7e6665fc` 上 exit 0，release metadata 精确绑定该提交，15/15 checksum 回读一致；Windows x64 raw EXE SHA-256 为 `CFE8B6684B2FAF6C0D1178B3515BDD2779116E6DDCEED251C72ABECC6A41B626`，NSIS 安装态 SHA-256 为 `01B3181D5775FE06FFA11C31246B66F3AF03118197F7C3E5D4247719EB408791`。
- 事务 `ccsm-20260815-055216-f59d4af5f0354cb6a76f17614b090351` 在独立隐藏 PowerShell 进程内完成 preflight、备份、kill、卸载、安装、拉起与健康检查；新进程 PID `55436`，`/health` 返回 HTTP 200，FileVersion/ProductVersion 均为 `3.19.1-26`，运行中 EXE 哈希与预期 NSIS 安装态哈希精确一致。
- 最终新会话无模型名 canary 全部通过：Flash child `01a00247-28f8-7ae0-9240-fa20a8eac5b4` 自动选择 `deepseek-v4-flash`，真实执行 `rg`/`git status` 并完成 follow-up，DeepSeek native Responses HTTP 200；Pro child `01a0024a-1feb-7212-907e-04cb8655a742` 自动选择 `deepseek-v4-pro` 完成源码/提交审查，Responses→Chat bridge HTTP 200；官方保留 child `01a0024e-5404-7f90-ae0b-ec76026f067b` 选择内置 `default` 与 `gpt-5.6-sol`，ChatGPT Codex Responses HTTP 200。
- GitHub Actions run `31846073316` 的 Windows x64/ARM64、Linux x64/ARM64、macOS 五个平台构建，以及 `Publish GitHub Release`、`Assemble latest.json` 共七个 jobs 全部 success。Release 非 draft、非 prerelease，并由 GitHub `releases/latest` 指向 v26。
- Release 包含 19 个实际资产。全部资产已重新下载并用本地 SHA-256 与 GitHub 服务端 digest 逐项比较，`DigestMismatches=[]`；`latest.json` 版本为 `3.19.1-26`，包含 `darwin-aarch64`、`darwin-x86_64`、`windows-x86_64`、`windows-aarch64`、`linux-x86_64`、`linux-aarch64` 六个 updater entry，所有 URL 均指向 v26，签名均非空。

## 已知非阻断边界

- 前端 catalog identity 的排序仍使用 `localeCompare`，与后端 NFKC + case-fold 尚未形成共享、完整、已测试的 Unicode 规范化契约；本轮没有加入不完整的 Unicode case-fold 补丁。
- Sub-Agent V2 reasoning/catalog 缺口已由真实 canary 证实并在 `80e7b04a` 根修；最终安装后的新会话 Flash、Pro、官方保留路径 canary 均已通过。未来修改 role 编译、父级 selection policy 或 catalog reasoning 契约时，仍须重跑这三条真实门禁，不能只用配置或单元测试替代。

## 升级与回滚

1. 从 GitHub Releases 下载与 `v3.19.1-26` tag 精确匹配的安装包，并核对平台、架构、签名与服务端 digest。
2. 退出 Codex/ChatGPT app-server 与 CCSwitchMulti 后正常覆盖安装；不要删除 CCSwitchMulti 数据库、`~/.codex` 或用户自定义 role TOML。
3. 启动 CCSwitchMulti，保存当前 Provider 或 MultiRouter 方案，完成状态工作台验证；重启 Codex 并新建会话。
4. 如需回滚，只下载已发布的上一版本安装包并通过事务安装器执行备份、卸载/安装、恢复和健康验证；不要手动删除配置或直接复制未知来源的数据库。

## 发布与下载证明

- Annotated tag `v3.19.1-26` 的 peeled commit 固定为 `7e6665fcfd297f3f8954850d59f9786831b1e3d2`；发布后的文档提交不会移动该 tag。
- GitHub Release：[CCSwitchMulti v3.19.1-26](https://github.com/BigStrongSun/ccswitchmulti/releases/tag/v3.19.1-26)，发布于 2026-08-14 23:00:28 UTC。跨平台构建、19 个资产、六平台 updater 签名、`latest.json` 与 GitHub digest 已由 [Release workflow](https://github.com/BigStrongSun/ccswitchmulti/actions/runs/31846073316) 和发布后下载回读双重验证。
