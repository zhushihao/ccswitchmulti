# 对话接手与产出复核报告

> 目标对话：`01a02dd1-29b4-7f02-bc2e-eaea9f6e5262`（标题：`你好，熟悉一下代码库`）  
> 复核时间：2026-08-23  
> 复核基线：`v3.19.2-16.2`，提交 `6923a99693ef38f8fbc25ff5042b58c0679eaa73`

## 一、结论先行

原对话最终产出的 `BUGS-remaining-2026-08-23.md` 有实际价值。报告列出的 8 条问题都能在当前代码中找到对应依据，其中 7 条属于功能、稳定性或界面一致性问题，1 条只是调试日志残留。

但是，原对话把静态代码检查写成了“整个代码库已经穷尽、其余全部干净”。现有证据不能支持这个结论。原对话没有给 7 个遗留功能问题建立能够失败的专门测试，也没有实际运行 Mac 安装包验证用户现场。因此，正确说法应当是：

- 原对话找到了 7 个可信的遗留缺陷和 1 个清理项。
- 原对话已经确认 Mac 崩溃修复代码进入了 16.2，相关前端回归测试通过。
- 原对话已经确认 16.2 的远程构建与发布成功。
- 原对话没有证明“除此之外再无 bug”，也没有证明 Mac 用户安装后一定不再出现任何问题。

## 二、原对话实际完成了什么

原对话完成了以下工作：

1. 原对话读取了仓库中的大量 Codex 路由、代理、同步、数据库和前端代码。
2. 原对话复核了 Mac 报错对应的修复提交 `839efa7c`。
3. 原对话复核了 Luna 401 对应的修复提交 `b458479b`。
4. 原对话查询了远程 Release 和构建状态。
5. 原对话在仓库中创建了 `BUGS-remaining-2026-08-23.md`。
6. 原对话没有修改业务源码，没有创建新提交，也没有修复报告中的 8 条遗留项。

当前报告文件仍然是 Git 未跟踪文件。原对话提到的 `PLAN-reasoning-narrow-wide.md` 的文件时间是 2026-08-15，早于本对话，因此不能算作本对话的新产出。当前其他未跟踪文档也没有足够证据可以归到本对话名下。

## 三、8 条遗留项逐条复核

### 1. 删除共享 Provider 时，删除路径会发布所有受影响路由

复核结果：**代码层面确认成立，缺少专门回归测试。**

更新路径会读取当前激活路由，并跳过非激活路由；删除路径在事务提交后直接遍历全部 `prepared` 路由并调用发布函数。两个路径的保护规则确实不一致。如果个人方案和公司方案共享同一个 Provider，非激活方案确实有机会覆盖共享的模型目录文件。

原报告把该问题定为中等严重度，判断基本合理。该问题值得优先补失败测试并修复。

### 2. `readCodexRouting` 直接调用 `v2.routes.map`

复核结果：**代码层面确认成立，属于输入防护缺口。**

前端会把任意 `schemaVersion: 2` 对象强制当成 `CodexRoutingConfigV2`，随后直接调用 `v2.routes.map`。当同步数据或手工数据缺少 `routes` 时，界面会抛出异常。当前已有测试只覆盖“路由缺少 `modelSelection`”，没有覆盖“配置缺少 `routes`”。

原报告把该问题定为低危可以接受，但是原报告最后又写“无崩溃类遗留”，这两个说法互相矛盾。

### 3. 三个次级界面的匹配规则没有复用运行时的 include 限制

复核结果：**代码层面确认成立。**

主投影路径已经实现“前缀命中后仍要检查 include 名单”。但是 `collectRoutedCatalogModels`、`routeMatchesModel` 和 `handlePreviewRoute` 仍然只判断“精确命中或前缀命中”。用户反选模型后，这三个界面仍可能把模型显示成可路由，而后端会拒绝该模型。

这是一组同源问题。后续修复应当抽出一个共享匹配函数，避免三个位置分别复制规则。

### 4. 推理档位清理没有同步处理 `default_effort`

复核结果：**代码层面确认成立，现有测试没有覆盖坏默认值。**

`sanitize_persisted_efforts` 会删除非法的 `supported_efforts` 和 `effort_map` 条目，但是函数不会修改 `default_effort`。后续 `validate` 明确要求默认档位必须仍在支持列表中。因此，当默认档位正好被清掉时，整个能力声明仍会被丢弃。

现有两个清理测试都使用合法默认值，所以测试全绿不能否定该问题。

### 5. 未拿到代理活动目标时，方案仍然回退到 `routingPlans[0]`

复核结果：**条件成立时确实存在，影响范围需要运行场景验证。**

当前工作台优先使用用户显式选择的方案，然后使用代理返回的活动 Provider，最后使用 `routingPlans[0]`。代理尚未处理请求时，活动 Provider 可能为空。上层已经读取了持久化的 `currentProviderId`，但是工作台没有接收该值。

原报告所说的“可能选到错误方案”是可信的。原报告所说的“对象顺序随机”表达过强，更准确的描述是“该顺序不是用户明确保存的当前方案，不能作为业务依据”。

### 6. `saveOrder` 保留多条 `console.error`

复核结果：**确认存在，但属于清理项，不应计成功能 bug。**

代码仍有多条 `[model-order]` 错误级日志。该问题会污染控制台和日志判断，但是不会直接改变业务结果。

### 7. 只有前缀的旧路由迁移后会得到空 include 名单

复核结果：**代码层面确认成立，缺少专门迁移测试。**

迁移函数只从旧路由的 `match.models` 计算模型名单，然后据此生成 `modelSelection`。函数稍后才读取 `match.prefixes`。当旧路由只有前缀时，迁移结果会得到空的 include 名单；新运行时又要求 include 模式的前缀命中必须同时进入 include 名单，所以这条路由会失效。

现有 5 个迁移测试没有覆盖“只有前缀、没有模型”的场景。

### 8. 路由预览仍提示“会走默认路由”

复核结果：**确认成立。**

前端预览在没有命中时仍提示请求会进入默认路由。后端 v2 运行时已经改成未命中就拒绝，不再使用默认路由兜底。该文案会直接误导用户。

该问题和第 3 条都来自前端匹配语义没有统一，但是第 8 条属于独立的结果文案错误，可以单独修复和测试。

## 四、远程发布状态复核

远程仓库 `zhushihao/ccswitchmulti` 的 Release 构建结论是正确的：

- Release 工作流 `32634370465` 已成功完成。
- Linux、Windows、Windows ARM64、macOS 和发布任务全部成功。
- macOS 构建、签名、公证检查全部成功。
- `v3.19.2-16.2` 是当前 latest Release。
- Release 标签正好指向提交 `6923a996`。
- Mac 修复 `839efa7c` 和 Luna 401 修复 `b458479b` 都包含在该标签中。

原报告的资产数字不准确。GitHub API 返回 19 个上传资产；GitHub 页面如果再算上两个自动生成的源码压缩包，会显示 21 项。19 个上传资产中还包含签名文件和 `latest.json`，因此“21 个安装包”这个说法是错误的。

## 五、本次接手后重新执行的验证

马博士重新执行了以下验证：

- 前端完整单元测试：144 个测试文件通过，1168 个测试通过，0 个失败。
- Codex 工作台测试：66 个测试通过，0 个失败。
- 路由修改 Rust 测试：6 个通过，0 个失败。
- 路由迁移 Rust 测试：5 个通过，0 个失败。
- 推理档位清理 Rust 测试：2 个通过，0 个失败。
- Git 标签包含关系：Mac 修复和 Luna 401 修复都在 16.2 中。
- GitHub Release：构建、Mac 签名、公证和发布任务全部成功。

这些测试证明当前基线没有触发现有测试覆盖的回归。这些测试没有覆盖第 1、2、4、5、7、8 条的关键失败条件，因此不能用“全部测试通过”来宣布这些问题不存在。

## 六、原对话执行质量问题

### 1. 对话出现严重重复循环

原对话有一个长时间运行的回合反复输出“停止重复读取并做最后检查”，但是同一表达重复了很多次。用户因此明确反馈“卡住了”。这个回合最终被中断。

### 2. 对话两次超过模型上下文上限

原对话后期两次因为输入超过 262144 tokens 而失败。用户两次要求“写排查报告”，任务仍然没有正常返回最终答复。根因是对话持续累积大段命令输出和重复推理，没有在合适的阶段压缩上下文或停止扩张排查范围。

### 3. 结论强度超过证据强度

原对话主要使用静态搜索和代码阅读。原对话没有为 7 个功能问题建立“修复前必定失败”的专门测试，也没有执行真实 Mac 安装包的现场复现。原对话仍然使用了“穷尽完毕”“其余全部干净”“无崩溃类遗留”等绝对结论。这些结论不严谨。

### 4. 报告内部存在矛盾

原报告第二条明确描述了一个前端崩溃路径，但是最终结论又写“无崩溃类遗留”。原报告第一条描述了共享目录覆盖风险，但是最终结论写法容易让人误以为不存在状态覆盖风险。

### 5. 资产统计把不同类型文件混为一谈

原对话把安装程序、签名文件、更新描述文件和源码包全部称为“安装包”。该口径会让世豪误判发布产物数量。

## 七、接手后的建议顺序

1. 先为第 1 条补一个“两个方案共享 Provider、只允许激活方案发布”的删除路径失败测试，然后修删除路径。
2. 同批修复第 2 条，并补“`schemaVersion: 2` 但缺少 `routes`”的前端回归测试。
3. 为第 7 条补“只有前缀的旧路由迁移”测试，然后明确迁移后应该使用 `All`，还是根据目录展开 include 名单。
4. 为第 4 条补“默认档位正好被清掉”的测试，再决定默认值清空还是回退。
5. 抽出统一的前端匹配函数，一次解决第 3 条和第 8 条，避免规则再次漂移。
6. 给工作台传入持久化的当前 Provider，解决第 5 条。
7. 最后清理第 6 条日志。

## 八、最终接手判断

原对话的核心产出可以作为修复清单使用，但是不能作为“仓库已经被完整证明安全”的质量证明。当前最稳妥的接手方式是保留这 8 条清单，修正报告中的绝对结论和发布数字，然后按照“失败测试先行、修复随后、原场景复验”的顺序逐条处理。
